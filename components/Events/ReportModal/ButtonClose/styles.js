import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
	button: {
		position: 'absolute',
		right: -20,
		top: 5,
		fontSize: 20,
		padding: 0,
		margin: 0,
		width: 'auto'
	}
});

export default styles;
