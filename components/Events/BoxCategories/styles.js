import { makeStyles } from '@material-ui/core/styles';

const styles = makeStyles({
	title: {
		marginTop: 40,
		paddingLeft: 10,
		textDecoration: 'none',
		display: 'block'
	}
});

export default styles;
